<h2>商品一覧</h2>

<% @items.each do |item| %>
  <%= attachment_image_tag item, :image %>
  <%= link_to item_path(item) do %>
      <%= item.name %>
    <% end %>
  
  <p>¥<%= item.price.to_s(:delimited) %></p>
<% end %>
  